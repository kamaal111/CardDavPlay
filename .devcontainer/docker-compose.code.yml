services:
  code:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
      args:
        NODE_VERSION: 20
        PYTHON_VERSION: 3.11
    container_name: carddav-play
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
